<?xml version="1.0" encoding="utf-8"?>
<qroot>
<question>
<Title>Calculate the fraction of wins in craps</Title>
<Body>&lt;p&gt;I want to write a function that takes a positive integer n as input, simulates n craps games, and returns the fraction of games the player won.&lt;/p&gt;&lt;p&gt;the underlying craps simulating code i&#x27;ve written is as follows:&lt;/p&gt;&lt;pre&gt;&lt;code&gt;import randomdef craps():    dice = random.randrange(1,7) + random.randrange(1,7)    if dice in (7,11):        return 1    if dice in (2,3,12):        return 0    newRoll = random.randrange(1,7) + random.randrange(1,7)    while newRoll not in (7,dice):        newRoll = random.randrange(1,7) + random.randrange(1,7)    if newRoll == dice:             return 1    else:        return  0import randomdef testCraps(n):    count = 0    fract = count/n    games = n*craps()    for i in range(games):        if i == 1:            count +=1        else:            pass    return fractusage:&amp;gt;&amp;gt;&amp;gt; fracCraps(10000) 0.4844&amp;gt;&amp;gt;&amp;gt; fracCraps(10000)0.492&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;what i get when I execute is:&lt;/p&gt;&lt;pre&gt;&lt;code&gt;&amp;gt;&amp;gt;&amp;gt; testCraps(10000)0.0&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;I just can&#x27;t get the counter to work.???&lt;/p&gt;</Body>
<Tags>python-3.x,count</Tags>
</question>
<answer>
<Body>&lt;p&gt;At the beginning of each iteration of your for-loop, you are resetting &lt;code&gt;countW&lt;/code&gt; to &lt;code&gt;0&lt;/code&gt;.  Set it to zero once outside the for loop to keep a running count.&lt;/p&gt;&lt;p&gt;You also want to move your calculation of &lt;code&gt;fract&lt;/code&gt; outside the for loop.  Calculate the &lt;code&gt;frac&lt;/code&gt; after all the looping is done. &lt;/p&gt;</Body>
</answer>
<answer>
<Body>&lt;p&gt;You&#x27;re getting &lt;code&gt;0&lt;/code&gt; as the result every time because you set &lt;code&gt;fract = countW/n&lt;/code&gt; when &lt;code&gt;countW&lt;/code&gt; is &lt;code&gt;0&lt;/code&gt;. &lt;code&gt;countW&lt;/code&gt; is &lt;strong&gt;always&lt;/strong&gt; &lt;code&gt;0&lt;/code&gt;, since you reset it each time through the loop. You just want to set it once, at the beginning of the function outside the loop. The same goes for &lt;code&gt;fract&lt;/code&gt;; you want that to be done at the end (or better yet, just &lt;code&gt;return countW/n&lt;/code&gt; at the end and eliminate &lt;code&gt;fract&lt;/code&gt; altogether).&lt;/p&gt;&lt;pre&gt;&lt;code&gt;import randomdef testCraps(n):    countW = 0    for i in range(n):        # The rest of your code    return countW/n# Example outputprint(testCraps(10000)) # 0.6972print(testCraps(10000)) # 0.698&lt;/code&gt;&lt;/pre&gt;&lt;hr&gt;&lt;p&gt;&lt;strong&gt;Edit @new content&lt;/strong&gt;&lt;/p&gt;&lt;p&gt;You seem to have edited your question to ask a different question, having only half-implemented the answers given here. You need to make sure you&#x27;re returning the right thing (&lt;code&gt;count/n&lt;/code&gt;), and make sure you&#x27;re calling things inside the loop when you mean to. &lt;code&gt;n*craps()&lt;/code&gt; is either &lt;code&gt;n&lt;/code&gt; or &lt;code&gt;0&lt;/code&gt;, since &lt;code&gt;craps()&lt;/code&gt; returns just a &lt;code&gt;1&lt;/code&gt; or a &lt;code&gt;0&lt;/code&gt;. What you want is, &lt;code&gt;n&lt;/code&gt; times, call &lt;code&gt;craps()&lt;/code&gt; and assess the outcome.&lt;/p&gt;&lt;pre&gt;&lt;code&gt;def testCraps(n):    count = 0    for _ in range(n):        if craps() == 1: # &quot;play&quot; a craps game and check the result            count +=1    return count/n# Example outputprint(testCraps(10000)) # 0.4971print(testCraps(10000)) # 0.4929&lt;/code&gt;&lt;/pre&gt;</Body>
</answer>
</qroot>
