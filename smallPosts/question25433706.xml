<?xml version="1.0" encoding="utf-8"?>
<qroot>
<question>
<Title>Qt Console app using QNetworkAccessManager</Title>
<Body>&lt;p&gt;I&#x27;m trying to write a &lt;code&gt;Qt&lt;/code&gt; app that calls a web service. This is a console app, and url will be passed in as a command line argument. I searched for example &lt;code&gt;http&lt;/code&gt; programs in &lt;code&gt;Qt&lt;/code&gt; and found this link:&lt;/p&gt;&lt;p&gt;&lt;a href=&quot;http://qt-project.org/doc/qt-5/qnetworkaccessmanager.html&quot; rel=&quot;nofollow&quot;&gt;http://qt-project.org/doc/qt-5/qnetworkaccessmanager.html&lt;/a&gt;&lt;/p&gt;&lt;p&gt;Here it has the following code example:&lt;/p&gt;&lt;pre&gt;&lt;code&gt;QNetworkAccessManager *manager = new QNetworkAccessManager(this);connect(manager, SIGNAL(finished(QNetworkReply*)),        this, SLOT(replyFinished(QNetworkReply*)));manager-&amp;gt;get(QNetworkRequest(QUrl(&quot;http://qt-project.org&quot;)));&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Now, if I take this and paste it into my console app, in &lt;code&gt;main&lt;/code&gt;, I obviously get build errors because &lt;code&gt;this&lt;/code&gt; does not exist. I get :&lt;/p&gt;&lt;pre&gt;&lt;code&gt;invalid use of &#x27;this&#x27; in non-member function&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Is there an equivalent &lt;code&gt;QNetworkAccessManager&lt;/code&gt; for non-GUI/console type apps?&lt;/p&gt;</Body>
<Tags>c++,qt,qnetworkaccessmanager</Tags>
</question>
<answer>
<Body>&lt;p&gt;&quot;this&quot; is the this pointer of an object, so in main.cpp it causes errors, you should write some class where you will work with network and after that use this class in main function&lt;/p&gt;&lt;p&gt;It should be something like this. When you run app, you&#x27;ll get html code of Qt site&lt;/p&gt;&lt;p&gt;It is just example, in future we can add here constructot, destructor, maybe signals(signals help us communicate with for example other classes if we need this)&lt;/p&gt;&lt;p&gt;*.h&lt;/p&gt;&lt;pre&gt;&lt;code&gt;#ifndef NET_H#define NET_H#include &amp;lt;QObject&amp;gt;#include &amp;lt;QtCore&amp;gt;#include &amp;lt;QNetworkAccessManager&amp;gt;#include &amp;lt;QNetworkReply&amp;gt;class Net : public QObject{Q_OBJECT  QNetworkAccessManager *manager;private slots:  void replyFinished(QNetworkReply *);public:  void CheckSite(QString url);};#endif // NET_H&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;*.cpp&lt;/p&gt;&lt;pre&gt;&lt;code&gt;#include &quot;net.h&quot;void Net::replyFinished(QNetworkReply *reply) { qDebug() &amp;lt;&amp;lt; reply-&amp;gt;readAll(); }void Net::CheckSite(QString url) {  QUrl qrl(url);  manager = new QNetworkAccessManager(this);  connect(manager, SIGNAL(finished(QNetworkReply*)), this, SLOT(replyFinished(QNetworkReply*)));  manager-&amp;gt;get(QNetworkRequest(qrl));}&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;main&lt;/p&gt;&lt;pre&gt;&lt;code&gt;#include &quot;net.h&quot;int main(int argc, char *argv[]){    QApplication a(argc, argv);    Net handler;    handler.CheckSite(&quot;http://qt-project.org&quot;);    return a.exec();}&lt;/code&gt;&lt;/pre&gt;</Body>
</answer>
</qroot>
