<?xml version="1.0" encoding="utf-8"?>
<qroot>
<question>
<Title>Reading In Data From A Text File And Separating Out The Data Contained In Each Line</Title>
<Body>&lt;p&gt;In the program currently I wish to read in text from a text file line by line, extracting data from each line. Each line in the text file contains a letter such as “A” followed by a colon “:” followed by an integer such as “2”. The function in the program that should read in the data currently looks like this:&lt;/p&gt;&lt;pre&gt;&lt;code&gt;void readFile(){std::ifstream aFile;                                                                                                                                                                                std::string alldata;std::string letter;                                                                                                                 aFile.open(&quot;KeyPopularity.txt&quot;);while (std::getline(aFile, alldata)){                                                                                                               letter = alldata[0];    for (int i = 0; i &amp;lt; keyboardKeys.size(); i++)    {        if (keyboardKeys[i].character==letter)                                                                                              {            keyboardKeys[i].noOfPresses = int(alldata[2]);                                                                                  }        std::cout &amp;lt;&amp;lt; letter &amp;lt;&amp;lt; &quot; &quot;;        std::cout &amp;lt;&amp;lt; int(alldata[2]) &amp;lt;&amp;lt; &quot;\n&quot;;    }}aFile.clear();                                                                                                                      }&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;However the alldata variable at position 0 (alldata[0]) instead of listing just a single character ie the letter lists a number such as 87 before the letter eg. 87 W as opposed to just W. The question is why is this happening and is there a way to extract the data from each line in the text file line by line to obtain the letter at position 0 of a line string and the number at position 2 of the line string?&lt;/p&gt;</Body>
<Tags>c++,string,io,ifstream</Tags>
</question>
<answer>
<Body>&lt;p&gt;Some observation/mistakes in your program:&lt;/p&gt;&lt;ul&gt;&lt;li&gt;letter is a std::string, however you assign a character to it; this is allowed but are you sure that this is the intent? You can use char instead of string.&lt;/li&gt;&lt;li&gt;alldata[2] is a char. For the line &quot;A:2&quot; for example, the value will be &#x27;2&#x27; (with ascii value 50, which is not the same as value 2). You cannot simply convert it to int with int(). To convert a character of a digit (&#x27;0&#x27;..&#x27;9&#x27;) to its respective integer value you need to subtract the value 48 from the ascii code (or &#x27;0&#x27;).&lt;/li&gt;&lt;li&gt;also change type of keyboardKeys[i].character to char instead of std::string&lt;/li&gt;&lt;/ul&gt;&lt;p&gt;You can rewrite your code like this:&lt;/p&gt;&lt;pre&gt;&lt;code&gt;void readFile(){    std::ifstream aFile;    std::string alldata;    char letter;    aFile.open(&quot;KeyPopularity.txt&quot;);    while (std::getline(aFile, alldata))    {        letter = alldata[0];        for (int i = 0; i &amp;lt; keyboardKeys.size(); i++)        {            if (keyboardKeys[i].character == letter)            {                keyboardKeys[i].noOfPresses = alldata[2] - &#x27;0&#x27;;            }            std::cout &amp;lt;&amp;lt; letter &amp;lt;&amp;lt; &quot; &quot;;            std::cout &amp;lt;&amp;lt; alldata[2] - &#x27;0&#x27; &amp;lt;&amp;lt; &quot;\n&quot;;        }    }    aFile.clear();}&lt;/code&gt;&lt;/pre&gt;</Body>
</answer>
</qroot>
