<?xml version="1.0" encoding="utf-8"?>
<qroot>
<question>
<Title>How to change multiple rows in a column from unicode to timestamp in python</Title>
<Body>&lt;p&gt;I am learning python for beginners. I would like to convert column values from unicode time (&#x27;1383260400000&#x27;) to timestamp (1970-01-01 00:00:01&lt;code&gt;enter code here&lt;/code&gt;). I have read and tried the following but its giving me an error.&lt;/p&gt;&lt;pre&gt;&lt;code&gt;ti=datetime.datetime.utcfromtimestamp(int(arr[1]).strftime(&#x27;%Y-%m-%d %H:%M:%S&#x27;);&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Its saying invalid syntax. I read and tried a few other stuffs but I can not come right.. Any suggestion?&lt;/p&gt;&lt;p&gt;And another one, in the same file I have some empty cells that I would like to replace with 0, I tried this too and its giving me invalid syntax:&lt;/p&gt;&lt;p&gt;&lt;code&gt;smsin=arr[3];        if arr[3]=&#x27;&#x27; :            smsin=&#x27;0&#x27;;&lt;/code&gt;Please help. Thank you alot.&lt;/p&gt;</Body>
<Tags>python-2.7,unix-timestamp</Tags>
</question>
<answer>
<Body>&lt;p&gt;You seem to have forgotten a closing bracket after &lt;code&gt;(arr[1])&lt;/code&gt;.&lt;/p&gt;&lt;pre&gt;&lt;code&gt;import datetimearr = [&#x27;23423423&#x27;, &#x27;1163838603&#x27;, &#x27;1263838603&#x27;, &#x27;1463838603&#x27;]ti = datetime.datetime.utcfromtimestamp(int(arr[1])).strftime(&#x27;%Y-%m-%d %H:%M:%S&#x27;)print(ti)# =&amp;gt; 2006-11-18 08:30:03&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;To replace empty strings with &#x27;0&#x27;s in your list you could do:&lt;/p&gt;&lt;pre&gt;&lt;code&gt;arr = [&#x27;123&#x27;, &#x27;456&#x27;, &#x27;&#x27;, &#x27;789&#x27;, &#x27;&#x27;]arr = [x if x else &#x27;0&#x27; for x in arr]print(arr)# =&amp;gt; [&#x27;123&#x27;, &#x27;456&#x27;, &#x27;0&#x27;, &#x27;789&#x27;, &#x27;0&#x27;]&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Note that the latter only works correctly since the empty string &lt;code&gt;&#x27;&#x27;&lt;/code&gt; is the only string with a &lt;a href=&quot;https://docs.python.org/2/library/stdtypes.html#truth-value-testing&quot; rel=&quot;nofollow&quot;&gt;truth value&lt;/a&gt; of &lt;code&gt;False&lt;/code&gt;. If you had other data types within &lt;code&gt;arr&lt;/code&gt; (e.g. &lt;code&gt;0&lt;/code&gt;, &lt;code&gt;0L&lt;/code&gt;, &lt;code&gt;0.0&lt;/code&gt;, &lt;code&gt;()&lt;/code&gt;, &lt;code&gt;[]&lt;/code&gt;, ...) and only wanted to replace the empty strings you would have to do:&lt;/p&gt;&lt;pre&gt;&lt;code&gt;arr = [x if x != &#x27;&#x27; else &#x27;0&#x27; for x in arr]&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;More efficient yet would be to modify &lt;code&gt;arr&lt;/code&gt; in place instead of recreating  the whole list. &lt;/p&gt;&lt;pre&gt;&lt;code&gt;for index, item in enumerate(arr):    if item = &#x27;&#x27;:        arr[index] = &#x27;0&#x27;&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;But if that is not an issue (e.g. your list is not too large) I would prefer the former (more readable) way.&lt;/p&gt;&lt;p&gt;Also you don&#x27;t need to put &lt;code&gt;;&lt;/code&gt;s at the end of your code lines as Python does not require them to terminate statements. They can be used to delimit statements if you wish to put multiple statements on the same line but that is not the case in your code.&lt;/p&gt;</Body>
</answer>
</qroot>
