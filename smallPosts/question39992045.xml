<?xml version="1.0" encoding="utf-8"?>
<qroot>
<question>
<Title>Django model choice field Select a valid choice. That choice is not one of the available choices</Title>
<Body>&lt;p&gt;I have a model choice field(Many to many relation), When I try to submit the form it throws an error &lt;code&gt;Select a valid choice. That choice is not one of the available choices.&lt;/code&gt;&lt;/p&gt;&lt;p&gt;I have also added in &lt;code&gt;forms __init__&lt;/code&gt; but still facing same error. I think the error is due to string, integer in checkbox value. Can anyone help me to fix this one. Posting my code here&lt;/p&gt;&lt;pre&gt;&lt;code&gt;class CustomSignupForm(forms.ModelForm):    telephone = forms.CharField(label=_(&#x27;Telephone&#x27;), required=False)    def __init__(self, *args, **kwargs):        self.request = kwargs.pop(&#x27;request&#x27;, None)        super(CustomSignupForm, self).__init__(*args, **kwargs)        self.fields[&#x27;categories&#x27;] = forms.ModelChoiceField(queryset=Category.objects.filter(status=True), widget=forms.CheckboxSelectMultiple)        for name, field in self.fields.iteritems():            attr = {&#x27;class&#x27;: &#x27;custom-class&#x27;}            if field.label:                attr[&#x27;placeholder&#x27;] = field.label            field.widget.attrs.update(attr)&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Categories is the field with many to many selection, which is not allowing me to submit the form.&lt;/p&gt;&lt;p&gt;&lt;strong&gt;Model&lt;/strong&gt;&lt;/p&gt;&lt;pre&gt;&lt;code&gt;class Category(DateUpdate):    title = models.CharField(max_length=256,null=True,blank=False)    status = models.BooleanField(default=True)    def __unicode__(self):        return self.title&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;&lt;strong&gt;Form&lt;/strong&gt;&lt;/p&gt;&lt;pre&gt;&lt;code&gt;&amp;lt;div class=&quot;form-group&quot;&amp;gt;    &amp;lt;label&amp;gt;        {{ form.categories.label }}    &amp;lt;/label&amp;gt;    {% for check_choice in form.categories %}            {{ check_choice.tag }}            {{check_choice.choice_label}}    {% endfor %}    {% if form.categories.errors %}        &amp;lt;span class=&quot;error&quot;&amp;gt;            {{ form.categories.errors.as_text }}        &amp;lt;/span&amp;gt;    {% endif %}&amp;lt;/div&amp;gt;&lt;/code&gt;&lt;/pre&gt;</Body>
<Tags>python,django</Tags>
</question>
</qroot>
