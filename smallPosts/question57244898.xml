<?xml version="1.0" encoding="utf-8"?>
<qroot>
<question>
<Title>PYGAME: How to remove object element when objects collide?</Title>
<Body>&lt;p&gt;I&#x27;m new to programming and currently taking a fundamental course with zero prior knowledge. My team&#x27;s current project is to program a game in python. It is a remake of the classic snake game where you eat the object and grow larger while avoiding colliding with yourself. &lt;/p&gt;&lt;p&gt;The current issue im having is that I&#x27;m adding in another &#x27;snack&#x27; that will minus a tail instead of adding if the wrong block is eaten. How would I go about removing a &#x27;tail&#x27; element if the player collides into the wrong element?&lt;/p&gt;&lt;p&gt;Here is also a github link to the full code:&lt;a href=&quot;https://github.com/GroupProject701/Snek/blob/master/Snek&quot; rel=&quot;nofollow noreferrer&quot;&gt;https://github.com/GroupProject701/Snek/blob/master/Snek&lt;/a&gt;&lt;/p&gt;&lt;p&gt;I assumed that using &lt;code&gt;remove()&lt;/code&gt; or &lt;code&gt;kill()&lt;/code&gt; would remove a &#x27;tail&#x27; from the &#x27;snake&#x27; but i&#x27;m having difficulties understanding how to execute the functions and executing the proposed idea of the project.&lt;/p&gt;&lt;p&gt;Here is also a github link to the full code:&lt;a href=&quot;https://github.com/GroupProject701/Snek/blob/master/Snek&quot; rel=&quot;nofollow noreferrer&quot;&gt;https://github.com/GroupProject701/Snek/blob/master/Snek&lt;/a&gt;&lt;/p&gt;&lt;p&gt;this is the code that is used to append an extra &#x27;tail&#x27; block to the player and is working properly&lt;/p&gt;&lt;pre&gt;&lt;code&gt;    def addCube(self):        tail = self.body[-1]        dx, dy = tail.x, tail.y        if dx == 1 and dy == 0:            self.body.append(cube((tail.pos[0]-1,tail.pos[1])))        elif dx == -1 and dy == 0:            self.body.append(cube((tail.pos[0]+1,tail.pos[1])))        elif dx == 0 and dy == 1:            self.body.append(cube((tail.pos[0],tail.pos[1]-1)))        elif dx == 0 and dy == -1:            self.body.append(cube((tail.pos[0],tail.pos[1]+1)))        self.body[-1].x = dx        self.body[-1].y = dy# ADD TAIL CODE ABOVE HERE------------------------&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;This is the code that we tried to execute the plan to remove a &#x27;tail&#x27; block from the player if they collide into this object.&lt;/p&gt;&lt;pre&gt;&lt;code&gt;    def minusCube(self):        tail = self.body[-1]        dx2, dy2 = tail.x, tail.y        if dx2 == 1 and dy2 == 0:            self.body.remove(cube((tail.pos[0]-1,tail.pos[1])))        elif dx2 == -1 and dy2 == 0:            self.body.remove(cube((tail.pos[0]+1,tail.pos[1])))        elif dx2 == 0 and dy2 == 1:            self.body.remove(cube((tail.pos[0],tail.pos[1]-1)))        elif dx2 == 0 and dy2 == -1:            self.body.remove(cube((tail.pos[0],tail.pos[1]+1)))        self.body[+1].x = dx2        self.body[+1].y = dy2# MINUS TAIL CODE ABOVE HERE FOR PP3---------------------&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;We expect the &#x27;snake&#x27; to lose a tail block when it collides with the new object, but instead we&#x27;re getting errors from &lt;code&gt;remove()&lt;/code&gt; and this is most likely due to lists not defined.&lt;/p&gt;</Body>
<Tags>python,python-3.x,pygame</Tags>
</question>
</qroot>
